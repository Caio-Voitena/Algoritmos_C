#include <stdio.h>
#include <stdlib.h>
// //Escreva um algoritmo para validar um lote de recibos bancários. O programa deverá inicialmente solicitar o valor (soma)
// total do lote e o número de recibos. A seguir deverá ler o valor de cada recibo calculando o valor (soma) total.
// Após a digitação dos valores de todos os recibos, o programa deverá imprimir as seguintes mensagens: “LOTE Ok” se a soma
// informada for igual a soma calculada; “Diferença negativa” se a soma calculada for menor que a informada; e
// “Diferença positiva” se a soma calculada for maior que a informada. Observação: O valor da diferença deve ser impresso(caso exista).
int main (){
    



    return 0;
}